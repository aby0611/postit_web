<% header = link_to('All posts', posts_path) + " &raquo; Showing category: #{@category.name}".html_safe %>
<%= render 'shared/content_title', title: header %>

<% @posts.each do |post| %>
  <%= render 'posts/post', post: post %>
<% end %>
